
- job:
    name: familylife-forms-api
    description: |
      FamilyLife Forms API (ASP.NET Web API)
    project-type: multibranch-pipeline
    branch-sources:
      - github:
          credentials-id: FL-Jenkins-GitHub-User
          owner: CruGlobal
          repository: FamilyLife-FormsAPI
          behaviors:
            discover-branches:
            filter-by-name-with-wildcards:
              include: master staging

- job:
    name: familylife-aop-api
    description: |
      FamilyLife Art of Parenting API (.NET Core).
      Code changes to the staging branch are automatically built and deployed to the staging environment.
      Code changes to the master branch are automatically built and but require manual approval before deployment to the production environment.
    project-type: multibranch-pipeline
    branch-sources:
      - github:
          credentials-id: FL-Jenkins-GitHub-User
          owner: CruGlobal
          repository: FamilyLife-ArtOfParentingAPI
          behaviors:
            discover-branches:
            filter-by-name-with-wildcards:
              include: master staging              

- job:
    name: scale-of-belief-lambda
    description: |
      Scale-of-belief lambda/serverless project.
    project-type: multibranch-pipeline
    branch-sources:
      - github:
          credentials-id: Cru-Jenkins-GitHub-User
          owner: CruGlobal
          repository: scale-of-belief-lambda
          behaviors:
            discover-branches:
            filter-by-name-with-wildcards:
              include: kma-6-jenkins-deployment
